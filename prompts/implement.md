# STS Implement Prompt

Read `SPEC-TO-SHIP.md`, load the relevant STS skill, and work inside the active `.spec-to-ship/features/<feature-id>/` namespace.

Use TDD for behavior changes. Plan and record evidence for each acceptance criterion, including negative/boundary cases and runtime/demo/generated-artifact proof where applicable. Do not check off upstream tasks until current evidence proves the exact item.
